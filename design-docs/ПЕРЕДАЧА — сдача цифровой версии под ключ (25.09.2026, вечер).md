# ПЕРЕДАЧА — сдача цифровой версии под ключ (25.09.2026, вечер)

Дизайнер (Влад): главное — чтобы цифровая версия игралась от и до, весь
текст читался, ничего не вылезало, всё открывалось и нажималось.

## Состояние рабочей копии (НЕ ЗАКОММИЧЕНО)

Правки в main-клоне, не закоммичены:
- `HotSeatWindow.java`:
  - зона игрока — 40% высоты окна (230–340), пересчёт при изменении окна, пока разделитель не тронут мышью;
  - у вкладок ящиков есть минимальная высота;
  - заголовок хода сжимается;
  - поле с отступом сверху (`setTopReserve(92)`);
  - выбор между картами — церемонией с лицами (arsenal_draw2, keep_objective, objective_keep);
  - cu_sides — призраком ЦУ на гексе;
  - 35 новых заголовков в KIND_LABELS и `kindLabel()` вместо служебного имени;
  - лента через `FeedWords.ru`, `feedLog` для тестов.
- **СБОРКА СЛОМАНА:** в KIND_LABELS дубль `Map.entry("move_source", "откуда двигаться")` — ExceptionInInitializerError.
  - **Первое дело:** удалить ту строку, которую добавил я (в блоке после `cu_sides`). Затем проверить остальные дубли: `awk '/KIND_LABELS = Map.ofEntries/,/\);/' … | grep -oE 'Map.entry\("[a-z_0-9]+"' | sort | uniq -d`.
- `kp/PlayerTable.java`:
  - ряд зоны вписывается по ширине (блок «ВЕСЬ РЯД ВПИСЫВАЕТСЯ ПО ШИРИНЕ»);
  - стопка «сыграно» на низком окне стала плашкой.
- `kp/KpTab.java` — кегль подгоняется под высоту вкладки.
- `kp/ChoiceWords.java`:
  - подписи по виду варианта: free_objective_burn, spec_arsenal_use, модули red/blue_slot/replace, move_red/blue, order_spec, attack (`ChoiceWords.attack`);
  - пасы module_place_*, move_source, maneuver_hex;
  - словарь английских фраз ENGLISH; номера модулей R30-1 → «модуль».
- `kp/FeedWords.java` (новый) — лента по-русски.
- `FieldView.java` — `setTopReserve`.
- `StartMenuWindow.java` — «Штаб» в палитре стола (`Theme.applyTable`), колонка в JScrollPane, тёмное поле и галерея, галерея в границах contentPane.
- `StartMenuShot.java` — offscreen; `HotSeatShot.java` — `-Dshot.specs`.
- `Offscreen.java` — служебные окна без кнопки на панели задач (Window.Type.UTILITY).
- engine `ЦуИзЗапаса.java` — в контекст cu_sides добавлен "hex".
- `HotSeatUiClickTest.java`:
  - проверка сырого текста: RAW и checkText по вариантам, картам выбора, заголовку и ленте;
  - в список карточных видов добавлены arsenal_draw2, keep_objective, objective_keep.

## Что сделать дальше

1. Починить дубль, скомпилировать: `mvn -o -q -pl engine,cards,bots,gui -am install -DskipTests`.
2. Прогнать `HotSeatUiClickTest` и дочистить всё, что он перечислит как «служебный текст на экране». Список — в `gui/target/surefire-reports/kelium.gui.HotSeatUiClickTest.txt`.
3. Прогнать весь `mvn -o test` и закоммитить **только эти файлы**. Не `git add -A`: в дереве лежат чужие .kmap и html.
4. Снять все виды решений на 1366×768 вчетвером.
   - Скрипт: `scratchpad/qa/run.sh 1366 768 "human,punisher:1,supplier:1,builder:1" p4s`. Он копирует классы в `qa/cls`, поэтому во время съёмки компилировать можно.
   - Просмотреть снимки, особенно редкие виды: landing, ricochet_target, reaction, destroyed_pay (подписи с BuildingToken@…), seal_move, storage_side, module_*.
5. `destroyed_pay` и `ricochet_target`: у вариантов payload — объекты-жетоны, а подписи вида «token worth 1» и «power_plantL3 игрока 0». Нужны нормальные слова.
6. Пересобрать exe: `powershell -NoProfile -ExecutionPolicy Bypass -File make-exe.ps1`. Затем доложить Владу скриншотами.

## Фоновые помощники (worktree, НЕ слиты)

- **Супер-задания 9.0.0** по новым шаблонам «экспорт-задания-супер» (секции «по ★» и «по ★★»). Ветка `worktree-agent-a11de6aad71e7d64b` в `.claude/worktrees/`.
- **Арсенал:**
  - начальные №5–6 и пустой шаблон;
  - супер «Штабная директива», «Келемиевый рудник» и три пустых шаблона;
  - обычный №33.
  - Ветка `worktree-agent-a3421d626c66049de`.
- Когда закончат — просмотреть лица, слить (`git merge`), прогнать тесты.
- **Супер-задания ГОТОВЫ:** коммит 5051739 в ветке `worktree-agent-a11de6aad71e7d64b`.
  - 12 карт s9_01…s9_12, тесты зелёные.
  - Лист лиц: `.claude/worktrees/agent-a11de6aad71e7d64b/design-docs/СУПЕР-ЗАДАНИЯ 9.0.0 — лист лиц.png`.
  - Заметка для Влада с таблицей и 7 вопросами — там же: `СУПЕР-ЗАДАНИЯ 9.0 — что придумано и почему (25.09.2026).md`.
  - Вёрстку книги не перерисовывали: `reflow.py` смотрит в `C:\shared\forged-in-kelium`, запускать после слияния.
  - Нужно: слить, проверить лица глазами, показать Владу.

## Вопросы Владу (открыты)

- У начальных арсеналов №5 и №6 напечатаны чужие названия («Полевой генератор», «Мародёрка»). На №6 значок «−1 монета», а в тексте «2 монеты».
- На арсенале №32 напечатан старый приказ «РАЗРАБОТКА».
- Глава 4 книги: арсенал «рубашкой в ячейке под планшетом войск» противоречит правилу «рука без предела, стопка под хранилищем».
- Убрать ли старый пересказ правил из справочника реплея.

Решено сегодня: у предложения рынка две ячейки при любом числе игроков (уже в main, коммит 3d95840).

## ИТОГ (25.09.2026, поздний вечер) — пункты 1–6 сделаны

- Дубль в KIND_LABELS убран; тест кликов зелёный (лента: энергия, журнал
  без пути, реакции словами; скобки «[…]» в ленте — тире).
- Подписи `destroyed_pay` и `ricochet_target` — словами по жетону
  (`ChoiceWords.tokenRu`). Пузырь вариантов больше не режет подписи;
  заголовок хода сокращается многоточием (FlatLaf сам его не ставил).
- Съёмка 1366×768 вчетвером: 33 вида сняты, 14 за 4 сида не встретились
  (attack, build_neutral, combat_victim, destroyed_pay, energy_or_modules,
  exchange_where, keep_objective, landing, module_gild_pick, module_keep,
  module_place_blue, objective_keep, pay_power, tower_hex) — снять с другими
  сидами.
- Супер-задания 9.0.0 слиты в main; лица в полном размере — на Диске,
  «Общие компоненты\экспорт-суперзадания\супер-задание-NN.png».
- Решения вечера (журнал решений): пути конца партии, снос ЦУ за модуль
  блокировки, прокрутка рынка под низ, 4–10 карт рынка, супер-задание №10.
- Ветка помощника «Арсенал» (worktree-agent-a3421d626c66049de) брошена
  НЕЗАКОММИЧЕННОЙ — не сливать вслепую, разобрать отдельно.
- Открыто: что с военной победой вдвоём (сейчас её нет — «втроём
  и вчетвером»).
